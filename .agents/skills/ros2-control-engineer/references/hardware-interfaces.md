# Hardware interfaces

Compare `<ros2_control>` against what the plugin exports: joint names, state
interfaces, command interfaces, initial values and lifecycle state.

For real hardware, confirm emergency stop, operation mode, scaling, limits,
watchdogs and a clear workspace. Use mock hardware for controller/MoveIt
integration and physics simulation for contact or dynamics.
